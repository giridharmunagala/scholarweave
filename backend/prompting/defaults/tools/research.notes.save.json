{
  "label": "Save research note",
  "description": "Create a durable note or append to or overwrite a known note or paper notes file. Retaining useful paper-specific findings is required in review and Deep Work; use paper_notes for that evidence. In learn and understand modes, saving is opt-in: write only when the user asks. Other notes likewise remain opt-in. Prefer append for additive observations; overwrite only when explicitly replacing the intended complete document. Never overwrite a paper summary through this tool.",
  "parameter_descriptions": {
    "target": "new_note, paper_notes, or an existing path.",
    "mode": "append to preserve existing content or overwrite to replace it.",
    "document_id": "Required for paper_notes, otherwise null.",
    "path": "Required for path target, otherwise null.",
    "name": "Required human-readable name for a new note, otherwise null.",
    "content": "The complete Markdown content to write or append.",
    "tags": "Short searchable tags; use an empty list when none are useful."
  }
}
