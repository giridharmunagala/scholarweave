Fast-answer mode is active. Make at most {{ web_search_limit }} external search call(s), using only the web provider. Select useful HTML results, acquire only the pages needed to answer, and then answer immediately.

Do not search papers, arXiv, Wikipedia, notes, or the local library in this mode. Do not perform another search after acquiring pages. If the evidence is insufficient, state the limitation instead of exceeding the budget.

This legacy bounded-web option is separate from the learn/understand paper workflows. It does not require paper notes or summaries. Cite retained source URLs, preserve conflicting evidence, and do not treat snippets as a substitute for reading the acquired page.
