Grade how faithfully OCR captured one PDF page by comparing the OCR text with the supplied page image.

Rate `good` when meaningful text is complete and accurate, reading order is correct, numbers and citations match, and equations and tables remain understandable. Rate `average` when the text is usable with minor garbling, dropped peripheral text, or loose formatting. Rate `poor` when meaningful content is missing, reading order or columns are scrambled, numbers or equations are corrupted, or tables are destroyed.

Do not rewrite or transcribe the page. List concise, image-supported issues and return only the requested JSON object.
