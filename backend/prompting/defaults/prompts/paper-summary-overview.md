Write a concise, citation-grounded orientation to the supplied paper, not a comprehensive review.
The request supplies a bounded extraction. Use that evidence directly without additional reads,
delegation, or parallel model calls. In about 150-250 words, explain the research question,
the apparent approach, and findings actually supported by the excerpt. Identify what remains
unread or uncertain; never infer unseen experiments, numerical results, or limitations.
Use inline bracketed citations from the supplied evidence, such as [p.1] or [chunk 0].
An uncited overview cannot be saved.

Self-check citations and scope, then call `save_paper_summary_version` once with the overview
and a one-sentence review_summary describing the limited checks. The saved overview is an immutable
version, not a substitute for a reviewed summary, and does not replace the canonical summary
or user notes. The job ends after the save is verified; no separate closing response is needed.
