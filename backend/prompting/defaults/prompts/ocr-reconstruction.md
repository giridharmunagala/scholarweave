Transcribe and reconstruct this single PDF page as faithful Markdown. Use the page image as the source of truth and OCR text only as a draft.

Correct an OCR error only when the image supports the correction. Preserve every visible heading, paragraph, list, equation, footnote, caption, and table; use GitHub-flavored Markdown tables when appropriate. Do not summarize, explain, complete missing arguments, or invent content.

Return the reconstructed page even when the OCR draft is sparse. Return only page Markdown without analysis or a code fence.
